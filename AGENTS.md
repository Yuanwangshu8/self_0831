# 个人笔记库维护规范

本仓库是用户与 AI 共同维护的 Obsidian 个人笔记库。所有操作以保留可追溯性、减少无关扩散、便于长期检索为先。

## 初始化状态

当前目录结构已初始化：

| 目录 | 职责 |
| --- | --- |
| `01-事实库/` | 保存可核验的原始资料、观察和更正记录。 |
| `02-推理区/` | 基于事实库形成范围明确的分析、判断和决策。 |
| `03-日记/` | 保存按日期组织的个人经历、感受和复盘。 |
| `04-工作任务/` | 管理待办、执行状态、交付物和周计划。 |
| `05-项目/` | 保存有目标、有周期的项目总览、资料和复盘。 |

根目录及以上每个内容目录均有 `index.md`。`.obsidian/` 是 Obsidian 配置目录，除非用户明确要求，否则不要修改其中内容。

## 目录与索引规则

1. 每个新建文件夹必须同时新建 `index.md`，不可遗漏。
2. `index.md` 必须列出该文件夹中的文档名称，并提供约 200 个汉字的目录内容概括；文档增删后同步更新索引。
3. 新笔记应放入职责最匹配的现有目录。只有确有必要时才创建新目录，并先更新上级目录的 `index.md`。
4. 文件名优先使用清晰、稳定的格式：日期类使用 `YYYY-MM-DD`；任务使用 `任务-主题.md`；项目使用 `项目-项目名.md`；事实记录使用 `来源记录-YYYY-MM-DD-主题.md` 或 `观察记录-YYYY-MM-DD-主题.md`。
5. 关联内容使用 Obsidian 内部链接；优先链接到原始事实，而非重复复制全文。

## 信息分层规则

### 事实库

- 只保存可追溯的事实、来源摘录、原始数据和直接观察。
- 每条记录应包括记录日期、信息来源或证据位置；不确定内容标记为“待核验”。
- 已写入的原始事实不得直接改写或删除。发现问题时，新增更正或补充笔记并链接原记录。
- 不在此处写推测、行动计划或长篇个人感受。

### 推理区

- 每篇笔记只围绕一个明确问题、判断或决策。
- 明确区分事实、假设、推导和结论，并链接所依据的事实库笔记。
- 标注待验证项、信心程度或下一步验证动作；新证据出现后可修订推理。
- 不把无关灵感堆进同一篇笔记，避免推理无限发散。

### 日记、任务与项目

- 日记保留真实的个人记录，可链接后续行动，但不必改写成正式报告。
- 任务应包含目标、状态、下一步行动、复查或截止时间，以及交付物链接。
- 项目是导航和管理页，关联任务、事实和推理；不要重复复制各处内容。
- 项目完成后保留成果、重要决定和简短复盘。

## AI 操作要求

1. 修改前先判断内容属于事实、推理、日记、任务还是项目；归属不明时向用户确认。
2. 不得把 AI 的猜测写成事实。推测必须进入推理区，并标明依据和不确定性。
3. 不删除用户笔记、不覆盖原始事实；需要调整时采用新增、补充、链接或显式更正的方式。
4. 每次新增、移动、删除文档或文件夹后，检查并同步更新受影响目录的 `index.md`。
5. 使用简洁中文，尊重既有命名和链接；除非用户要求，不改动 `.obsidian/` 配置。
6. 每次有文件被修改、新增或删除后，都需要提交，并推送到远程仓库。
